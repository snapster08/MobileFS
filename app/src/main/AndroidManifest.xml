<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobilefs.seminar.pdfs.service">

    <!-- For file picker -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="mfs.ui.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="mfs.ui.JoinGroupActivity"
            android:label="@string/title_activity_join_group"
            android:parentActivityName="mfs.ui.EnterNameActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mfs.ui.HomeActivity" />
        </activity>
        <activity
            android:name="mfs.ui.CreateGroupActivity"
            android:label="@string/title_activity_create_group"
            android:parentActivityName="mfs.ui.EnterNameActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mfs.ui.HomeActivity" />
        </activity>

        <!-- File Picker Activity -->
        <activity
            android:name="com.nononsenseapps.filepicker.FilePickerActivity"
            android:label="@string/title_activity_file_picker"
            android:theme="@style/FilePickerTheme">
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service android:name="mfs.service.BackgroundService"></service>

        <activity
            android:name="mfs.ui.EnterNameActivity"
            android:label="@string/title_activity_enter_name"
            android:parentActivityName="mfs.ui.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mfs.ui.HomeActivity" />
        </activity>
    </application>

</manifest>